
let initialState = {
    round: {
        questions: [
            {text: "da ili net",answer: "da"}
        ]
    }
};


export const gameReducer = (state = initialState, action) => {
return state;
}